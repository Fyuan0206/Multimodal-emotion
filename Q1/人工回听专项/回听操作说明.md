> **音频核查更正：这两段原始视频的音轨全部为零，无法回听。** 不是播放器静音或复制丢失音轨。全量100条解码检查仅发现这2条数字静音；副本与原附件SHA256一致。请停止对这6词填写参考时间，不能靠转写猜测。该两段全部24个自动词边界均缺乏语音依据，应排除准确率计算；如需恢复，必须取得确实含语音且时间对应的原始素材。审计见“音频信号审计.csv/json”。以下原回听安排仅留作历史记录。

# Q1 人工回听操作与记录说明

本次只复核2条视频中的6个跨平台边界差异词。请亲自听音频，填写实际听到的开始与结束时间。预计完整播放一遍约12秒，精细定位需要多次回听。本文件夹独立于原始结果；填写前的记录均为空，不表示已完成核验。

## 先打开哪个文件

双击 `回听填写.html`，用浏览器播放并填写。视频文件在同目录的“素材”文件夹，不要单独移动HTML。填写完成后点击“下载填写结果”，保存JSON并交回；也可以直接编辑 `回听记录.csv`，两种方式任选一种，不必重复填写。网页不会自动保存，关闭或刷新前务必下载。

## 需要听多少秒

所有时间都从本文件夹内完整视频的0秒开始，单位为秒；不是网络长视频的时间，不需要加偏移。

| 视频 | 先完整播放 | 重点反复回听范围 | 需要记录的词 |
|---|---|---|---|
| 视频1.mp4 | 0.000–6.103秒 | 0.30–5.70秒 | or、more、hands |
| 视频2.mp4 | 0.000–5.731秒 | 0.20–5.10秒 | therapists、and、chiropractors |

重点范围只是宽范围的导航提示，不是参考答案。若实际词不在范围内，回到完整视频寻找。视频1音频有效终点6.0545秒，视频2为5.7016875秒；少量容器尾部不属于语音。

视频1给定转写：There are a dozen or more hands on techniques that we can do

视频2给定转写：So physical therapists and chiropractors will do joint manipulations and mobilizations

视频2要标的是 therapists 后、chiropractors 前的第一个 and，不是 manipulations 后的第二个 and。给定转写只是定位线索；如果实际发音和转写不同，请记录不同，不能强行对齐。

## 怎么确定开始和结束

1. 先用1倍速完整听一遍，理解上下文，再用0.75或0.5倍速反复听目标词附近。
2. 开始时间：该词第一个可辨认语音声学成分的起点。结束时间：最后一个可辨认语音成分的终点。不要把词前后长静音算进去。
3. 时间按原视频时间轴记录。0.5倍速时不需要乘2或除2，播放器显示的仍是媒体时间。
4. 连读、弱读、背景声或静音使边界难判断时，不要猜。选择“边界不确定”并在备注写清原因；未听到该词则选择“未听到目标词”，时间留空。
5. 可以先暂停并使用页面按钮填入当前时间，再微调。精细定位建议用可显示波形和时间选区的软件打开原视频或导出的音轨。不要仅凭点击反应时间填写毫秒边界。
6. 秒数可保留2到3位小数，精度须符合实际定位能力；小数位多不代表准确。填写后分别回放边界前后约0.2秒，确认没有漏音或带入相邻词。

## 每行要记录什么

| 字段 | 填写方式 |
|---|---|
| sample_id、word_index、word | 已填好，不修改。word_index为从0开始的源词索引。 |
| reference_start_s | 听到的词起点，如1.230，单位秒。这里只是格式示例，不是任何目标词答案。 |
| reference_end_s | 听到的词终点，必须大于起点，且不超过对应音频有效终点。 |
| review_status | confirmed＝已确认；uncertain＝边界不确定；not_heard＝未听到；transcript_mismatch＝转写不符；pending＝尚未听。 |
| confidence | high／medium／low，表示你对边界判断的把握，不是模型分数。 |
| reviewer、review_date | 实际回听人姓名或代号，实际日期。 |
| playback_rate | 主要使用的回听速度，如1、0.75或0.5。 |
| reviewer_note | 连读、静音、噪声、听到的不同单词、只能确定一端等情况。无问题可写“无”。 |

仅确认一端时，两列时间保持为空，把已知一端记在备注；不要补造另一端。“边界不确定”允许记录暂定的成对时间，但后续默认不进入已确认误差统计。

## 填完后我会计算什么

对已确认且有效的行，分别计算CPU和A100结果相对人工参考的起点误差、终点误差，以及每词最大边界误差 max(|预测起点−参考起点|, |预测终点−参考终点|)。报告单位为毫秒，并列明未核验、未听到及转写不符的行。

这6个词是专门挑出的差异样本，不能用它们估计全量100条视频的总体精度，也不能凭6词P90宣称整个数据集P90≤100ms。原有10条视频的独立人工核验计划仍保留。本次不改写原始特征或覆盖已有人工记录；是否需要重新提取受影响样本，依据回听结果另行判断。

本次词边界回听也不验证视觉特征的情绪表达能力。不要在此表中凭印象填写情绪结论。

## 交回方式

优先交回页面下载的 `Q1_六词回听记录.json`；或交回填好的CSV。保留文件名和列名。暂未听清的词如实标记即可，不必为凑齐6行而填写时间。
