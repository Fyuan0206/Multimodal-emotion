# Q1 交付状态：静音修正版 q1-1.2

> **新增模型辅助验证：** 见 [模型辅助验证/README.md](模型辅助验证/README.md)。两份材料按真实机器来源保存，不是人工结果。10段中5段ASR与给定文本差异很大，属于待查疑点；当前除了缺少人工边界真值，还存在转写/素材一致性未决风险。工程流程完成不代表数据语义和边界已验证准确。

## 使用哪一套结果

正式工程输出：`outputs/server_a100/`。正式图片：`figures/server_a100/`，使用其中EPS；04是同目录Excel表格。旧版本已归档至 `archive/pre_silence_fix/`，旧CPU输出和 `figures/paper/` 仅作历史材料。不要混用新旧覆盖率。

## 修正后的实测统计

100条、1932词：自动对齐1895词（98.0849%），未对齐37词，其中24词来自两段全零音频，13词含不支持的字符；低CTC分数词360个。该比例仍是自动输出覆盖率，不是准确率。新的人工复核子集为10条有声片段、198词，参考仍为空。

## 本轮修复

- 对解码音频记录全零标志、非零采样数、峰值和RMS；仅判定数字静音，不把非零信号等同于语音。
- 静音片段不运行CTC，不填词起止时间与置信分数，保留`audio_all_zero`原因。
- 静音片段原生声学特征置零、声学有效掩码false、有效声学时长为0；所有词级音频/视觉特征置零，词级对齐及两模态有效掩码false，token时间为−1。
- 原生视觉特征及时间、BERT文本与原转写均保留。不能因音频缺失而推断视觉也不可用。
- 100条完整重跑，不混用旧缓存；37项提取测试覆盖静音跳过模型、无效声学帧及极低非零信号不被任意判为全零。
- 修复Slurm直接提交脚本时工作目录指向临时目录的问题。
- 有声回听包从98条非全零音频中按时长选10条，不再要求回听原先静音的6词。

## 论文能写到哪一步

可写：特征提取、时序映射、缺失掩码、静音处理、自动结构核验、重复性检查及探索性视觉评估。词覆盖率只表示算法输出比例，不表示准确率。

不能写：全部词边界经过人工核验、总体P90≤100ms、15维几何充分表达情绪，或七类表情预测已在本数据集得到真实表情标签验证。

视觉增强已实跑100条。表情表示MAE0.607118，原几何0.598157，合并0.597896；配对描述性区间均跨0，没有可靠提升，因此保留为补充实验，不自动替换主模型。视觉评价使用原生帧，不依赖被撤销的静音词边界；新旧原生表示一致由发布审计检查。

## 唯一需要真人继续完成的证据

打开 `有声回听专项/回听填写.html`，回听实际音频、记录真实词边界并下载JSON。可分批提供，不确定词保留uncertain。没有参考就不计算准确性数字，不模拟人类标注。

10条是按时长覆盖选择的子集；完成后只能报告该子集准确性，不能未经抽样论证推广到全量。旧静音回听记录保留作问题追溯。

## 可复核记录

- `outputs/server_a100/validation.json`：全量结构与池化重算。
- `outputs/server_a100/silence_release_audit.json`：独立解码检查、新旧98条全数组及100条原生视觉/文本对照。
- `outputs/server_a100/reproducibility.json`：5条重复提取逐数组一致。
- `outputs/server_a100/human_review_metrics.json`：尚无人工参考的真实状态。
- `logs/server_figure_audit.json`：七图字体、碰撞、PNG和对齐检查。
- `有声回听专项/素材来源.json`：10条有声WAV的音频能量、来源与哈希。
- `Q1_A100_results.receipt.json`：交付压缩包哈希与文件数量。

本轮不修改论文正文，不推送GitHub。工作目录没有Git分支；上传范围为Q1代码、说明及核验产物，不含私钥、虚拟环境、大模型权重和原始视频。原数据和权重需依照来源另行准备。

## 本轮完成核查

服务器重跑任务1529268成功（5分27秒），发布核查与回听包任务1529289成功（52秒）。本地47项测试通过（37项提取、10项回听导入），服务器37项提取测试通过；5条重复提取逐数组一致。独立全量解码与新旧数组比较通过，7张图片审计通过。两次失败启动记录保留在日志中：工作目录与CUDA环境问题均已修复后成功重跑。
