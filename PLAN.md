# 问题1方案：多模态情感特征提取与时序对齐

## 1. 任务目标与样本定义

目标是把附件1的100条原始视频片段转换为可追溯、可复现的文本、语音、视觉时序特征，并提供跨模态对齐证据。样本主键定义为

$$
q_i=(video\_id_i, clip\_id_i),\qquad i=1,\ldots,100.
$$

本地清点显示：`label-100.xlsx` 有100条样本记录，素材分布在37个 `video_id` 子目录中，每条文件路径为 `附件1/<video_id>/<clip_id>.mp4`。因此不得用Excel行号或 `video_id` 单独标识样本。文本采用工作簿提供的转写文本；情感标签只保留为数据追溯字段，不输入Q1特征提取过程，也不用于Q2/Q3训练、调参或阈值选择。

## 2. 数学模型与特征定义

对第 `i` 个样本，记视频为 `V_i(t)`、音频为 `a_i(t)`，文本词序列为 `W_i=(w_{i1},...,w_{iJ_i})`。目标是得到三组特征序列及其统一的文本token轴表示：

$$
H_i^T\in\mathbb{R}^{L_i\times768},\quad
H_i^A\in\mathbb{R}^{L_i\times27},\quad
H_i^V\in\mathbb{R}^{L_i\times15}.
$$

其中 `L_i` 为token序列长度（优先统一为50；先检查全部文本长度，若有超过48个内容WordPiece的样本，则扩展统一长度以避免截断，并如实报告）。所谓“统一/标准化时序特征”是指统一采样、对齐、张量维度组织和填充规则；归一化模型输入另行保存，不能覆盖原始特征。

### 2.1 文本特征：BERT上下文表示

模型选择 `bert-base-uncased`，取12层Transformer最后一层的768维隐状态；使用题目附件中的转写，不另做ASR。固定分词器版本、模型revision/hash、`add_special_tokens=True`、`max_length=L`、attention mask及token offset。文本特征为

$$
h^T_{ij}=\operatorname{BERT}_{last}(W_i)_j\in\mathbb{R}^{768}.
$$

选择base模型的理由是其输出是词/子词级序列特征、与MOSEI常用特征形态一致且CPU推理可行；不微调模型，不使用外部情感语料。

### 2.2 语音特征：27维可复现声学子集

采用调研综述建议的轻量实现：PyAV将音轨解码并重采样为16 kHz单声道；Praat/parselmouth以50 ms步长提取基频 `F0` 与浊音标记 `v∈{0,1}`；librosa从同一音频时间格提取25维MFCC。每个声学帧为

$$
h^A_k=[F_{0,k},v_k,c_{k1},\ldots,c_{k25}]\in\mathbb{R}^{27}.
$$

MFCC固定参数计划为采样率16 kHz、`n_fft=400`（25 ms窗）、`hop_length=800`（50 ms步长）、`n_mels=40`、`n_mfcc=25`；F0固定 `time_step=0.05`、`pitch_floor=50` Hz、`pitch_ceiling=500` Hz，VUV由有效F0帧定义。音频重采样只用PyAV，不用librosa重采样。该27维向量是“COVAREP式近似子集”，不是完整COVAREP 74维；报告中必须列明其余47维未复现，不把二者等同。

### 2.3 视觉特征：可解释的面部几何代理

选用OpenCV YuNet人脸检测器，输入缩放至320×320；在约15 Hz（按呈现时间戳每约1/15秒取最近帧）运行检测。检测框与5个关键点按原始帧宽高归一化，定义每帧特征顺序为

$$
h^V_r=[x/W,y/H,w/W,h/H,score,x_1/W,y_1/H,\ldots,x_5/W,y_5/H]\in\mathbb{R}^{15}.
$$

其中前4维为检测框，接着1维为检测置信度，后10维为5个面部关键点坐标。特征有效性由独立检测mask表示；未检出人脸的帧置零但不得解释为“中性表情”。选择YuNet是因为工具轻量、CPU可用、输出可逐维定义；这15维是面部几何代理，不称为FACET-35，也不声称是经过标注验证的AU情绪强度。

### 2.4 词区间与跨模态池化

设强制对齐器给词 `w_ij` 的区间为 `I_ij=[s_ij,e_ij)`，其内部声学/视觉采样帧集合分别为 `K_ij`、`R_ij`。对应词级特征为

$$
\bar h^A_{ij}=\frac{1}{|K_{ij}|}\sum_{k\in K_{ij}}h^A_k,\qquad
\bar h^V_{ij}=\frac{1}{|R_{ij}|}\sum_{r\in R_{ij}}h^V_r.
$$

若区间没有采样点，则取时间最近帧并设置 `nearest_frame_fallback=true`；不得静默填入普通均值。用BERT tokenizer的offset/word ID把同一词的池化语音、视觉特征散射给对应WordPiece。`[CLS]`、`[SEP]`及padding位置的音频/视觉特征置零，配套保存 `text_mask`、`audio_valid_mask`、`vision_valid_mask`、`padding_mask`。这样既能复查每个token对应的原视频时间，也能区分自然零值、补零和人脸漏检。

## 3. 模型选择与预检实验

| 子任务 | 选定方案 | 选择理由及边界 |
|---|---|---|
| 文本 | BERT-base-uncased，最后层768维 | 与MOSEI常用词级BERT特征形态相同，CPU可运行；只作特征提取，不微调 |
| 语音 | Praat F0/VUV + librosa 25维MFCC | 参数可公开复现，组成27维；明确是COVAREP近似，不虚称74维完整复现 |
| 视觉 | OpenCV YuNet + 15维归一化面部几何 | 轻量、可解释且逐维有定义；不是FACET官方35维 |
| 强制对齐 | 以P2FA/MOSEI词级规则为对齐参照；在隔离环境预检P2FA与可运行的MFA候选，最终选择须通过下述实测门槛 | 调研记录指出MFA在Windows需conda且G2P受限，WhisperX/MMS/NeMo受torchaudio环境问题影响；当前不预设未经验证的工具版本 |

**强制对齐器预检**：按短/中/长片段、不同说话速度及含专有名词等条件选10条样本；人工标注词起止时间作为参考，必要时由两人独立标注并复核分歧。对每个可运行候选记录词覆盖率、边界绝对误差、未对齐词率、每条耗时、OOV数量及重复运行一致性。边界误差定义为

$$
e_{ij}=\max(|s_{ij}-s^*_{ij}|,|e_{ij}-e^*_{ij}|).
$$

候选通过门槛建议设为：修正词典后词覆盖率≥98%，人工参考边界误差P90≤100 ms，且100条可在固定CPU环境完整处理。若都未通过，先解决依赖/词典/OOV并重测，不以均匀分词伪造时间戳。最终在配置文件中冻结工具、模型、版本、命令、词典修订及参数。

## 4. 全量实验设计

### 实验A：样本覆盖与媒体完整性

对100行清单逐一检查 `(video_id,clip_id)` 唯一、MP4存在且唯一、音视频可解码、文本非空、时间戳单调。记录视频/音频各自时长和流起点偏移、帧率、帧数、采样数、文件大小及SHA-256。任何问题都要有错误代码、原因和处理记录，不删除样本。

### 实验B：特征提取与维度审计

全量运行三个特征提取器，生成每条样本的原生序列与token级对齐序列。检查无NaN/Inf、各维顺序固定、mask长度匹配、padding位置符合约定。抽取附件1与附件2的18条重合样本，以及附件4可追溯的20条样本做结构校验。BERT特征在文本及tokenization相同条件下与附件2对应特征比较最大绝对误差；声学、视觉自定义维度不与官方74/35维作逐元素等同性宣称，只比较时间采样、有效长度、填充和统计范围。

### 实验C：对齐有效性与复现性

先完成10条人工边界核验，再对全量统计词覆盖率、低置信词数、边界误差分布、无帧区间和最近帧回退次数。将同一软件/模型/参数下的5条样本重复运行，比较数组最大差异和时间戳差异。报告失败率和修复前后数字，不只展示成功案例。

### 实验D：交叉核验和典型样本

使用调研报告识别出的18条附件1/附件2重合记录核对文本和时间组织；另用附件4的20条样本核对流程。附件4编号匿名化，必须依据源路径、元数据或长度签名匹配，容许并记录重编码导致的±1帧误差。典型样本按固定规则选择：时长接近100条中位数、对齐通过门槛且人脸检测覆盖较高。展示连续若干词的文本、词区间、对应音频帧范围、视频帧时间/帧号及三模态特征摘要；另选低置信边界样例说明局限。

## 5. 文件组织、复现与结果汇总

代码留在代码仓库；原始视频不入库。下列批量输出可放在 `outputs/Q1/`，但应加入Git忽略规则；提交时另打无损压缩包。

- `manifest.csv`：100行，样本键、原始路径/哈希、原始有效时长、状态。
- `features/<video_id>__<clip_id>.npz`：原生和对齐的文本/语音/视觉float32数组、mask、有效长度、时间戳、token信息。读取：`d = numpy.load(path, allow_pickle=False)`。
- `alignment/<video_id>__<clip_id>.json`：逐词文本、token映射、起止时间、置信度、源帧数量、回退标记。
- `summary.csv`：100样本×3模态，共300行；列为样本编号、模态、原始有效时长、特征维数、原生/对齐有效长度、对齐粒度、padding规则及处理状态。
- `run.jsonl`、`environment.json`：完整命令、Python及依赖版本、模型revision/hash、关键参数、硬件环境、错误和修复日志。

特征文件按样本保存，汇总表按模态列出；预期每条对齐序列为文本 `(L,768)`、语音 `(L,27)`、视觉 `(L,15)`。原生声学序列为20 Hz、视觉约15 Hz。所有输出保留原始提取值；后续模型若需z-score，应只在附件2训练集拟合变换参数。最终计算含代码、日志、说明和特征文件的附件总大小，确认≤50 MB；删除非必要中间缓存，不能以有损降维代替完整原始特征。所有材料不得包含队伍身份信息。

## 6. 预期结果与报告呈现

本方案预期交付：100条一一对应的特征文件、300行模态汇总表、100份逐词对齐记录、运行环境与日志、至少1个真实样本的三模态对齐展示。全量结果表最终应填入实测数字，包括各片段音视频有效时长、文本token数、音频/视频采样帧数、人脸有效帧率、特征维度、对齐粒度、词覆盖率及异常/回退数量。

| 验收项 | 目标或报告值 | 当前能否给出实际值 |
|---|---|---|
| 样本完整性 | 输入100条、输出100条、匹配率100%、无重复主键 | 文件清点显示100个MP4与100条表格记录；仍须运行自动逐项核验 |
| 特征维度 | 文本768、语音27、视觉15 | 方案定义值；须由实际程序验证 |
| 时序结构 | 语音20 Hz，视觉约15 Hz，文本token轴对齐；保存原生有效长度和padding mask | 方案定义值；实际采样数待运行统计 |
| 对齐质量 | 10条人工核验；报告词覆盖率、P50/P90边界误差、失败与回退数 | 不能预填，必须实测；P90≤100 ms为预检通过目标 |
| 可复现性 | 固定版本/哈希/参数/命令；5条重复运行差异可量化 | 环境冻结并执行后填写 |
| 提交规模 | 全部提交材料≤50 MB | 特征压缩包生成后实测 |

**结果表述原则**：上述100%覆盖率、P90误差门槛等是验收目标，不是已取得的实验结果。正文必须将“方法预期”与“实际观测”分开；在全量运行前不得写出虚构的平均时长、精度、特征统计或典型案例。

## 7. 赛题符合性结论

本方案逐项覆盖题目要求的原始处理流程、三模态特征定义和工具、跨模态对齐逻辑、100条样本全量汇总、原始特征附件、典型样本验证及可复现说明；也纳入总附件≤50 MB、身份匿名和数据来源限制。**现阶段属于可执行的方案设计，尚非已完成的赛题结果。** 提交前必须先通过10条强制对齐器预检、冻结全部精确版本与命令，再完成100条全量处理、交叉核验、附件大小核验，并把所有占位目标替换为实测值。

**调研依据**：`调研/多模态情感研究现状综述.md` 中Q1关于BERT、27维声学近似、YuNet、词级池化、20/15 Hz采样和38对校验集的实测及建议。公开论文指标只作方法选择背景，不作为附件1的预期成绩或调参目标。

